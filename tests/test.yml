---
- hosts: localhost
  connection: local 

  vars:
#    ansible_python_interpreter: /usr/bin/python3

  roles:
    - { role: ansible-role-shibboleth-idp-configurator, confs: ['edugain','haka','hakatest'] }
