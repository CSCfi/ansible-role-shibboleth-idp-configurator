---
- name: Configure Shibboleth IdP
  hosts: test

  vars:
    # ansible_python_interpreter: /usr/bin/python3
    shibbolethidp_fqdn: test.idp.com

  roles:
    - { role: CSCfi.jetty, when inventory_hostname != 'localhost' }
    - { role: ansible-role-shibboleth-idp-configurator, confs: ['install','edugain','haka','hakatest'] }
